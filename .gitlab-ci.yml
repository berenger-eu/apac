image: registry.gitlab.inria.fr/jgaupp/constify

stages:
  - build
  - test

build_constify:
  tags:
    - medium
  stage: build
  script:
    - mkdir build
    - cd build
    - cmake ..
    - make

test_constify:
  tags:
    - medium
  stage: test
  script:
    - make test
