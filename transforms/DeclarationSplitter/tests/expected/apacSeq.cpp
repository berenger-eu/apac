#include <functional>
#include <optional>
template <class T> T &invalid_ref() {
  T *ptr = nullptr;
  return (*ptr);
}

void func_apacSeq(long long n) { int a = 4; }

int main() {
  long n;
  n = 654;

  return 0;
}